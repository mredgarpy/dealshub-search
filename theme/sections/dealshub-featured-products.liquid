{% comment %}
  DealsHub — Featured Products Section
  Dynamic ITem Collection Section with products from local API feed
{/comment %}
{% assign embody = section.settings.enable_loading )_{
const options = {
  root_attr: [
+]
};
{% endif %}

